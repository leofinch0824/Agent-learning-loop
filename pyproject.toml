[project]
name = "agent-learning"
version = "0.1.0"
description = ""
authors = [
    {name = "leofinch0824",email = "achilescuber@gmail.com"}
]
requires-python = ">=3.12,<4.0.0"
dependencies = [
    "python-dotenv (>=1.2.3,<2.0.0)",
    "langgraph (>=1.2.11,<2.0.0)",
    "langgraph-checkpoint-sqlite (>=3.1.1,<4.0.0)",
    "mlflow (>=3.16.0,<4.0.0)",
    "openai (>=3.13.0,<4.0.0)",
    "mcp (>=2.2.0,<3.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.1.1,<10.0.0)",
    "pytest-asyncio (>=1.4.0,<2.0.0)"
]
