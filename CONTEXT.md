# 业务模型迭代学习项目

本项目围绕业务知识沉淀、可定制 Skill、多模态模型改进与训练数据生产学习 Agent 工程。以下术语用于区分业务依据、模型假设和经验证的改进成果。

## Language

**业务知识（Business Knowledge）**：有来源、适用范围和有效时间的业务规则、术语与已确认案例。模型提出但尚未确认的解释属于知识候选。
_Avoid_: 把所有对话历史统称为业务知识

**业务 Skill（Business Skill）**：面向一类业务任务、可定制和复用的操作方法，包含适用条件、业务知识引用、步骤与结果要求。提示词是其组成部分，Skill 也可包含示例和工具使用方法。
_Avoid_: 将 Skill 等同于一段提示词或整个 Agent

**业务流程（Business Workflow）**：围绕业务成果组织的一组任务、条件分支和人工决策。一个流程可使用多个业务 Skill，同一 Skill 可被多个流程复用。

**错例（Failure Case）**：模型或流程结果与可信业务判定不一致的案例。无可信判定的异常结果属于待核验案例。
_Avoid_: 把低置信度结果直接当作错题

**归因假设（Attribution Hypothesis）**：对错例成因的可检验解释，可以涉及输入、证据、业务规则、提示词、工具、模型能力或标注。未经对照实验和必要审核的解释不能视为已确认根因。
_Avoid_: 将模型自述原因称为根因事实

**优化候选（Optimization Candidate）**：为改善任务表现提出的提示词或 Skill 新版本，其收益尚需与基线比较。
_Avoid_: 在验证前称为优化成果

**Skill 自迭代（Skill Evolution）**：依据执行反馈提出、评估并选择 Skill 新版本的过程。业务规则本身的变更需要独立的业务依据。
_Avoid_: 用“自我学习”混指知识确认、提示词搜索和模型权重训练

**训练数据候选（Training Data Candidate）**：经发现或构造、可能有助于模型后训练的案例，尚需核验正确性、代表性及训练用途。
_Avoid_: 将所有错例或模型修正直接称为训练真值

**已审核训练样本（Reviewed Training Sample）**：完成指定质量核验、可追溯到业务案例和审核结论的训练素材。素材是否适合 SFT、偏好优化或某种 RL 目标，需要分别判定。

**模型后训练（Model Post-training）**：通过训练数据及学习目标更新模型参数的过程。提示词优化和 Skill 版本更新不属于模型后训练。

**迭代发布（Iteration Release）**：依据评估证据和适用范围选定、用于后续业务执行的一组版本。发布对象可以是提示词、Skill 或经训练后验证的模型。
